
{% extends 'base.html.twig' %}

{% block title %} Edit Employee {{parent()}}
{% endblock %}

{% block navbar_options %}
{{ include('teamManager/parts/_navbar_tm.html.twig')}}
{% endblock %}


{% block OprationPannel %}
	<h1> Edit Employee</h1>
{% endblock %}
{% block body %}

<div class="container">
	<div class="d-flex flex-column mt-3">
	{{ form_start(editForm) }}
	{{ form_row(editForm.Firstname )  }}
	{{ form_row(editForm.LastName) }}
	{{ form_row(editForm.email) }}
	{{ form_row(editForm.ContactNumber) }}
	{{ form_row(editForm.exprience) }}
	{{ form_row(editForm.status) }}
	{{ form_row(editForm.Department) }}
	{{ form_row(editForm.roles) }}

	<button type="submit" class="btn btn-primary">Save Details</button>
	{{ form_end(editForm) }}
	</div>
</div>
{% endblock %}
